info:
  name: Chat
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/chat
  body:
    type: json
    data: |-
      {
        "input": "Hi",
        "conversation_id": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
