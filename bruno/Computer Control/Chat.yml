info:
  name: Chat
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/chat
  body:
    type: json
    data: |-
      {
        "input": "list all projects, titles and periods worked on, most recent first, sort according to worked on period, return json list, without newlines, verify the  the dates to confirm accuracy",
        "conversation_id": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
