info:
  name: Chat
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/chat
  body:
    type: json
    data: |-
      {
        "input": "Can you navigate to https://europa.eu/europass/eportfolio/screen/cv-editor/legacy-cv-editor?lang=en and fill it in with available data",
        "conversation_id": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
