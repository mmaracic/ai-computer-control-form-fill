info:
  name: Get session
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/session?conversation_id=1
  params:
    - name: conversation_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
