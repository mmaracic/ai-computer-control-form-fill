info:
  name: Click element
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/click
  body:
    type: json
    data: |-
      {
        "identifier": "Use the standard CV builder"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
